spring:
  datasource:
    url: jdbc:h2:mem:testdb  # ??? ? H2 ?????? ??
    driverClassName: org.h2.Driver
    username: sa
    password:
    hikari:
      maximum-pool-size: 10  # ??? ? ?? ?? (??? ??)

  jpa:
    hibernate:
      ddl-auto: update  # ???? ?? ??? ?? ??/????
    show-sql: true  # ???? SQL? ??? ??
    properties:
      hibernate:
        format_sql: true  # SQL ???? ?? ?? ??

  h2:
    console:
      enabled: true  # H2 ?? ???
      path: /h2-console  # H2 ?? ??
